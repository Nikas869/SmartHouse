@model List<SelectListItem>

@foreach (var selectListItem in Model)
{
    <a href="@Url.Action($"Create{selectListItem.Value}", "Components", new { ViewBag.RedirectId })" class="waves-effect waves-light btn">Create @selectListItem.Text</a>
}